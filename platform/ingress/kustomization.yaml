apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: logfabric-ingressnginx
  annotations:
    description: "Manages all LogFabric cluster ingress resource"

resources:
  - ingress-nginx.yaml